
import { Node, Edge } from '@xyflow/react';

export const initialNodes: Node[] = [
  {
    id: '1',
    type: 'actionNode',
    position: { x: 250, y: 50 },
    data: { label: 'Start Action' },
  },
];

export const initialEdges: Edge[] = [];
